A standalone UDP-based stateless file server that can be accessed through appropriate calls from client using client interfaces. 
The file server store all of its data in an on-disk (filesystem image). 
This image contains the on-disk representation of data structures and files, and client can request CRUD operations on the server 
