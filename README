Team member: Yan (Adam) Zhang

1. On UChicago's Linux machine, needs to type export LD_LIBRARY_PATH=. before running to use the shared library
2. client.c is used for debugging. It contains dummy code copied from one of the public test and is not used.

